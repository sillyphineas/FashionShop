<?xml version="1.0" encoding="UTF-8"?>
<project name="FashionShop" default="default" basedir=".">
    <description>Builds, tests, and runs the project FashionShop.</description>
    <import file="nbproject/build-impl.xml"/>

<!--     Định nghĩa compile.classpath bao gồm các thư viện trong thư mục Libraries 
    <path id="compile.classpath">
         Thêm thư viện JAR từ thư mục lib 
        <fileset dir="lib">
            <include name="**/*.jar"/>
        </fileset>
    </path>



     Target compile 
    <target name="compile">
         Tạo thư mục build/classes nếu chưa có 
        <mkdir dir="build/classes"/>

         Biên dịch các file .java vào build/classes 
        <javac srcdir="src" destdir="build/classes">
            <classpath refid="compile.classpath"/>
        </javac>
    </target>

    <target name="clean">
        <delete dir="build"/>
    </target>

    <target name="run" depends="compile">
        <exec dir="bin" executable="launcher.exe">
            <arg file="${dist.jar}"/>
        </exec>
    </target>-->

</project>

